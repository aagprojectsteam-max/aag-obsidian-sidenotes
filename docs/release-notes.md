SideNotes 0.2.1 preserves the public `context-aware-paragraph-notes` identity and existing MIT notice. Adds corrupt-store preservation, verified temporary store saves, import preflight and recovery journals, duplicate-identity write guards, migration/recovery documentation and expanded synthetic regression coverage. Back up before migration; multi-file imports are not globally atomic and real-host migration/mobile acceptance remains pending. Runtime data is excluded from the three release assets.
